@model IEnumerable<ViennaRuns.RunningGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rg_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.rg_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.rg_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.rg_id })
        </td>
    </tr>
}

</table>
